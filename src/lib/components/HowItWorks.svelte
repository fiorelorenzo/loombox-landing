<section class="band alt" id="how">
	<div class="wrap">
		<p class="kicker reveal">How it works</p>
		<h2 class="reveal">A loom weaves many threads into one cloth.</h2>
		<p class="intro reveal">
			loombox weaves many parallel agent sessions into one shipped product, and keeps the whole
			thing on infrastructure you control.
		</p>
		<div class="pillars">
			<div class="pillar reveal">
				<div class="ic"><svg aria-hidden="true"><use href="#i-nodes" /></svg></div>
				<h3>Agents run where you want</h3>
				<p>
					Spin up parallel sessions on your laptop or a remote box over <code>ssh:</code>, each in
					its own isolated worktree. Same-folder safety and a cross-node ownership lease keep two
					sessions from ever colliding.
				</p>
			</div>
			<div class="pillar reveal">
				<div class="ic"><svg aria-hidden="true"><use href="#i-phone" /></svg></div>
				<h3>Steer from anywhere</h3>
				<p>
					Watch live output, approve tool calls, drop into an interactive terminal, and reply to a
					session from a desktop browser or an installable phone PWA. An attention inbox and push
					tell you the moment an agent needs you.
				</p>
			</div>
			<div class="pillar reveal">
				<div class="ic"><svg aria-hidden="true"><use href="#i-shield" /></svg></div>
				<h3>A relay you own, blind by design</h3>
				<p>
					Ship one Docker image, run your own Postgres-backed relay. It routes only opaque
					ciphertext and routing metadata, never your prompts, code, or files. See
					<a href="#relay" style="color:var(--accent-text);text-decoration:none">how</a>.
				</p>
			</div>
			<div class="pillar reveal">
				<div class="ic"><svg aria-hidden="true"><use href="#i-key" /></svg></div>
				<h3>Your agents, your keys</h3>
				<p>
					Works with ACP coding agents, Claude Code today, Codex and Gemini reserved. Device pairing
					and recovery codes are yours. Voice, when it lands, runs on keys you hold, not a
					maintainer's account.
				</p>
			</div>
		</div>
	</div>
</section>
